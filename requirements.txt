# Core
pyyaml>=6.0.1
jinja2>=3.1.3
click>=8.1.7
rich>=13.7.0

# Report generation (Markdown -> HTML, no WeasyPrint)
markdown>=3.5.2

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0

# Optional: AST parsing for reliable NatSpec (install separately)
# py-solc-x>=2.0.0
# solc-select>=1.0.0

# Static analysis (external tools installed separately)
# slither-analyzer
# aderyn (download prebuilt binary from GitHub releases)
